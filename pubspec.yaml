name: geo_maclab_2050
description: Professional LWD testing application with GPS and calibration.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_blue_plus: ^1.31.0
  fl_chart: ^0.63.0
  csv: ^5.1.1
  permission_handler: ^10.4.3
  intl: ^0.18.1
  vibration: ^1.7.6
  geolocator: ^9.0.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true